[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Generate JSON object for function calls"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.4",
    "pydantic>=2.13.3",
]

[tool.uv.workspace]
members = [
    "llm_sdk",
]
